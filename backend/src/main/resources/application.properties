# Spring Application Name
spring.application.name=backend

# Supabase PostgreSQL Connection (Render/Supabase Cloud config)
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://aws-0-eu-west-1.pooler.supabase.com:6543/postgres?sslmode=require}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres.oxkwycjmnrcowylhajnh}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:RubraDB30072026}

spring.datasource.driver-class-name=org.postgresql.Driver

# JPA & Hibernate Settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Google API Credentials for OAuth Flow
google.client.id=${GOOGLE_CLIENT_ID:123456789-exampleclientid.apps.googleusercontent.com}
google.client.secret=${GOOGLE_CLIENT_SECRET:example-client-secret}

# JWT Token Security Config
jwt.secret=${JWT_SECRET:defaultSecretKeyShouldBeAtLeast32BytesLongForSecurityReasons}

# CORS Allowed Origins
app.cors.allowed-origins=${ALLOWED_ORIGINS:http://127.0.0.1:5500,http://localhost:5500,http://localhost:8080,capacitor://localhost,http://localhost}

# Server Error Response Settings
server.error.include-message=always
